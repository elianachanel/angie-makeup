/** Fondos estáticos en CSS — sin animación JS para scroll más fluido */
export function AmbientGlow() {
  return (
    <div aria-hidden className="ambient-glow pointer-events-none fixed inset-0 z-0 overflow-hidden" />
  );
}
